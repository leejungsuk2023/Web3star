# Copy to android/keystore.properties (do not commit). Put .jks next to this file; storeFile is filename only.

storeFile=web3star-upload.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=upload
keyPassword=YOUR_KEY_PASSWORD
